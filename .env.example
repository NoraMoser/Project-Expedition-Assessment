# Database
# A PostgreSQL connection string is required to run this app.
# For local development, you can use Docker (see docker-compose.yml) or a hosted provider like Neon (neon.tech).
DATABASE_URL=postgresql://user:password@localhost:5432/pe_destinations